# TODO / 後回しタスク

要件定義の本筋とは別に、後で検討・対応するタスクの控え。

## 本番環境: Docker ネットワーク構成 & Traefik ルーティング(後で検討)

> ステータス: 未着手 / 後回し
> 起票: 2026-06-25

本番運用に向けて、以下を設計・整備する必要がある。

### Docker ネットワーク構成
- [ ] 管理画面(Next.js + Hono)・各 MC サーバー(itzg)・FTP コンテナのネットワーク分離方針を決める。
  - 管理画面 ↔ Docker socket 経由のオーケストレーションは維持。
  - MC サーバー間は基本的に相互通信不要 → 隔離するか検討。
- [ ] MC サーバーのゲームポート / RCON ポートの公開範囲(LAN のみ / 特定 IP)を整理。
- [ ] FTP コンテナと MC サーバー `/data` ボリュームの共有マウント方針の最終化。
- [ ] 内部用ネットワーク(管理通信)と外部公開ネットワーク(ゲーム接続)を分けるか検討。

### Traefik ルーティング
- [ ] Traefik を導入し、管理画面 Web UI へのリバースプロキシ / TLS 終端を構成。
- [ ] ホスト名ベース or パスベースのルーティング設計。
- [ ] 管理画面の HTTPS 化(証明書: 自己署名 or 内部 CA / Let's Encrypt など)。
- [ ] WebSocket(コンソールのリアルタイムログ)を Traefik 経由で通す設定。
- [ ] MC のゲームトラフィック(TCP/UDP)は Traefik を通すか、直接公開かを判断
      (Traefik の TCP/UDP エントリポイント利用の是非)。
- [ ] 認証 / アクセス制限を Traefik ミドルウェアでも二重化するか検討。

### 留意点
- これらは MVP(Phase 1〜7)完了後、本番デプロイ前に着手する。
- 現状の開発・要件定義フェーズではスコープ外。
